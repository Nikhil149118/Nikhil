#include <stdio.h>

int main()
{
    float radius = 6;
    float pi = 3.14159265;
    float area = pi * radius * radius;
    printf("area of the circle is %f\n", area);
    int h = 6;
    printf("volume of the cylinder is %f cubic meter\n", area * h);
    return 0;
}